import string
import sys
import heapq

input = sys.stdin.readline

def solution(S, K):
    # write your code in Python 3.6
    size = len(S)
    count = 0
    for i in range(1, size):
        for j in range(i, size):




print(solution('decade', 15))
#include <vector>
#include <iostream>
#include <limits>
#include <algorithm>
#include <cmath>
#include <string>

using namespace std;

int main()
{
	int input;
	cin >> input;
	vector<int> vMap(input);
	for (int i = 0; i < input; i++)
		cin >> vMap[i];



	return 0;
}